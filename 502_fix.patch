*** Begin Patch
*** Update File: server/index.js
@@
-import { accessLogMiddleware, getLogPaths, logApi, logError, logFrontend, logInfo, requestContext, requestCorrelationMiddleware } from './lib/logger.js'
+import * as logger from './lib/logger.js'
+
+const {
+  accessLogMiddleware,
+  getLogPaths,
+  logApi,
+  logError,
+  logInfo,
+  requestContext,
+  requestCorrelationMiddleware,
+} = logger
+
+const logFrontendSafe = typeof logger.logFrontend === 'function'
+  ? logger.logFrontend.bind(logger)
+  : (message, payload) => logApi(message, payload)
@@
 app.get('/diag/pixel.gif', (req, res) => {
-  logFrontend('frontend_stage', {
+  logFrontendSafe('frontend_stage', {
@@
 app.post('/api/client-logs', express.json({ limit: '64kb' }), (req, res) => {
@@
-  logFrontend(entry.message, entry)
+  logFrontendSafe(entry.message, entry)
*** End Patch
