*** Begin Patch
*** Update File: server/index.js
@@
-import { accessLogMiddleware, getLogPaths, logApi, logError, logFrontend, logInfo, requestContext, requestCorrelationMiddleware } from './lib/logger.js'
+import * as logger from './lib/logger.js'
+
+const accessLogMiddleware = logger.accessLogMiddleware
+const getLogPaths = logger.getLogPaths
+const logApi = logger.logApi ?? logger.logInfo ?? (() => {})
+const logError = logger.logError ?? console.error
+const logFrontend = logger.logFrontend ?? logger.logApi ?? logger.logInfo ?? (() => {})
+const logInfo = logger.logInfo ?? (() => {})
+const requestContext = logger.requestContext ?? ((req, _res, next) => next())
+const requestCorrelationMiddleware =
+  logger.requestCorrelationMiddleware ?? ((req, res, next) => {
+    const correlationId =
+      req.headers['x-correlation-id'] ||
+      req.headers['x-request-id'] ||
+      (globalThis.crypto?.randomUUID ? globalThis.crypto.randomUUID() : `cid-${Date.now()}`)
+    req.correlationId = correlationId
+    if (typeof res.setHeader === 'function') {
+      res.setHeader('X-Correlation-Id', correlationId)
+    }
+    next()
+  })
*** End Patch
