node_modules
dist
.env
.DS_Store
logging/*.log
!logging/.gitkeep
mysql_dumps/*.sql
!mysql_dumps/.gitkeep

.env*

# Persisted user-uploaded pictures are runtime data and must never be committed.
userimages/
